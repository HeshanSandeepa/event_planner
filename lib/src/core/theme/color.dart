import 'package:flutter/material.dart';

abstract final class AppColor {
  static const themeColor = Color.fromRGBO(180, 12, 12, 1.0);
  static const themeError = Color.fromRGBO(239, 12, 12, 1.0);

  static const backgroundColor = Colors.white;
  // static const groupColor = Colors.white;
  // static const groupBorderColor = Colors.black12;
}
